import React from "react-native"
import handleRemovePlayer from "./HandleRemovePlayer"

// Smoke test to make sure handleAcceptOrDeny() causes no
// errors.
test("handleRemovePlayer smoke test", () => {
    handleRemovePlayer()
})