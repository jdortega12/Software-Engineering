import React from "react-native"
import handleChangeRoster from "./HandleChangeRoster"

// Smoke test to make sure handleChangeRoster() causes no
// errors.
test("handleChangeRoster smoke test", () => {
    handleChangeRoster()
})
