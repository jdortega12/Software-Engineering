import React from "react-native"
import handleCreateTeam from "./HandleCreateTeam"

// Smoke test to make sure handleCreateAccount() causes no
// errors.
test("handleCreateTeam smoke test", () => {
    handleCreateTeam()
})
