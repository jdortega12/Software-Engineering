import React from "react-native"
import handleCreateAccount from "./HandleCreateAccount"

// Smoke test to make sure handleCreateAccount() causes no
// errors.
test("handleCreateAccount smoke test", () => {
    handleCreateAccount()
})
