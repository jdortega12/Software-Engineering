import React from "react-native"
import handleLogout from "./HandleLogout"

// Smoke test to make sure handleLogout() causes no
// errors.
test("handleLogout smoke test", () => {
    handleLogout()
})
